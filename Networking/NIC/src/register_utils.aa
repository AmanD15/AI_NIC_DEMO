// register 21 holds the packet count.
$module [incrementNumberOfPacketsTransmitted]
	$in () $out () $is
{
	$call accessRegister ($zero<1> 15 21 $zero<32>) (r)
        $volatile nr := (r + 1)
	$call accessRegister ($one<1> 15 21 nr) (ignore)
}
